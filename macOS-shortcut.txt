+ shortcut
- copy: Window + C
- paste: Window + V
- switch tab: Window + Tab
- close current window: Window + W
- minimize current window: Window + M

+ Command Line
- return to previous directory: cd .
- select all: Window + A

+ software
- Install Homebrew:
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"

+ Install NVM:
brew install nvm
source $(brew --prefix nvm)/nvm.sh

+ NodeJS
+ NVM

+ Install Simulator cho android
- mở app store -> tìm Xcode -> cài đặt
- khi npm start project react native -> gõ dấu ? để hiện danh sách option -> chọn i để chạy simulator trên IOS
